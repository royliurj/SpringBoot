spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3333/jdbc
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

#    默认不会被加载，需要关联配置
    maxActive: 20
#    schema:
#      - classpath:department.sql